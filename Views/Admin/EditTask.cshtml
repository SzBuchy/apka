@model EventManageApp.Models.Task

@{
    ViewData["Title"] = "Edytuj Zadanie";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h3 class="mb-0">Edytuj Zadanie</h3>
                </div>
                <div class="card-body p-4">
                    <form asp-action="EditTask" method="post">
                        <input type="hidden" asp-for="Id" />

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Nazwa Zadania</label>
                            <input asp-for="Name" class="form-control" required />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label">Opis</label>
                            <textarea asp-for="Description" class="form-control" rows="4" required></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Points" class="form-label">Punkty</label>
                                <input asp-for="Points" type="number" class="form-control" required min="0" />
                                <span asp-validation-for="Points" class="text-danger"></span>
                            </div>
                            <div class="col-md-6 mb-3 d-flex align-items-end">
                                <div class="form-check mb-2">
                                    <input asp-for="RequiresPhoto" class="form-check-input" type="checkbox" />
                                    <label asp-for="RequiresPhoto" class="form-check-label">Wymaga Zdjęcia/Wideo</label>
                                </div>
                            </div>
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4">
                            <a href="@Url.Action("Tasks")" class="btn btn-secondary">Anuluj</a>
                            <button type="submit" class="btn btn-primary btn-lg">Zapisz Zmiany</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
